# build.properties
#

# Specify the login credentials for the desired Salesforce organization
sf.source.username = ryan.clous@werner.com.dev
sf.source.password = 
sf.source.orgname = werner--dev

sf.target.username = ryan.clous@werner.com.qauat
sf.target.password =
sf.target.orgname = werner--qauat

sf.packageBuild = testapex


#sf.sessionId = <Insert your Salesforce session id here.  Use this or username/password above.  Cannot use both>
# sf.pkgName = <Insert comma separated package names to be retrieved>sf.packageBuild
sf.pkgName = ${sf.packageBuild}.xml
#sf.zipFile = <Insert path of the zipfile to be retrieved>
#sf.metadataType = <Insert metadata type name for which listMetadata or bulkRetrieve operations are to be performed>

# Use 'https://login.salesforce.com' for production or developer edition (the default if not specified).
# Use 'https://test.salesforce.com for sandbox.

sf.source.serverurl = https://test.salesforce.com
sf.target.serverurl = https://test.salesforce.com

sf.maxPoll = 20
# If your network requires an HTTP proxy, see http://ant.apache.org/manual/proxy.html for configuration.
#
